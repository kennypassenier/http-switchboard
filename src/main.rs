fn main() {
    // L0 is the walking skeleton: the modules exist and CI is green before
    // any feature lands. L1 replaces this with config loading (K10:
    // fail-closed, with the remedy in the message).
    println!("http-switchboard: no features built yet (milestone L0)");
}
